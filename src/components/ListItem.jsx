/**
 * Компонент ListItem - отдельный элемент списка
 * 
 * Props (параметры):
 * - item: объект с id и text элемента списка
 * - onDelete: функция обратного вызова для удаления элемента
 * 
 * Функции:
 * - Отображает текст элемента
 * - Предоставляет кнопку удаления
 */
function ListItem({ item, onDelete }) {
  /**
   * Обработчик клика на кнопку "Удалить"
   * Вызывает переданную функцию onDelete с id текущего элемента
   */
  const handleDelete = () => {
    onDelete(item.id)
  }

  return (
    <li className="list-item">
      <span className="item-text">{item.text}</span>
      <button
        onClick={handleDelete}
        className="btn-delete"
        aria-label={`Удалить элемент: ${item.text}`}
      >
        🗑️ Удалить
      </button>
    </li>
  )
}

export default ListItem
