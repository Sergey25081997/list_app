# 📋 Список элементов - React приложение (Lite версия)

Учебный проект для курса **"Разработка Web-приложений с AI"** в университете искусственного интеллекта.

## 📖 Описание

Это простое React приложение, которое демонстрирует базовые концепции:
- **Функциональные компоненты** (App.jsx, ListItem.jsx)
- **Хуки** (useState для управления состоянием)
- **Ребёндеринг** (автоматическое обновление UI при изменении состояния)
- **Обработка событий** (onClick для кнопок, onChange для input полей)
- **Массивы и операции с ними** (concat, filter, map)

## ✨ Функциональность

✅ **Добавление элементов**: Введите текст и количество, затем нажмите кнопку "Добавить"  
✅ **Удаление элементов**: Каждый элемент имеет кнопку "Удалить"  
✅ **Сброс формы**: После добавления поля очищаются автоматически  
✅ **Отзывчивый дизайн**: Работает на всех устройствах  

## 🚀 Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm или yarn

### Инструкции

1. **Перейдите в папку проекта**:
   \`\`\`bash
   cd list-app-lite
   \`\`\`

2. **Установите зависимости**:
   \`\`\`bash
   npm install
   \`\`\`

3. **Запустите приложение**:
   \`\`\`bash
   npm run dev
   \`\`\`

4. **Откройте в браузере**:
   Перейдите на `http://localhost:5173` (адрес может отличаться, см. консоль)

## 📁 Структура проекта

\`\`\`
list-app-lite/
├── src/
│   ├── components/
│   │   └── ListItem.jsx       # Компонент для отдельного элемента списка
│   ├── App.jsx                # Главный компонент приложения
│   ├── App.css                # Стили для App
│   ├── index.css              # Глобальные стили
│   └── main.jsx               # Точка входа приложения
├── public/                    # Статические файлы
├── index.html                 # HTML шаблон
├── vite.config.js            # Конфигурация Vite
├── package.json              # Зависимости проекта
└── README.md                 # Этот файл
\`\`\`

## 🎯 Ключевые концепции

### useState - Управление состоянием

\`\`\`javascript
const [textInput, setTextInput] = useState('Default')
const [countInput, setCountInput] = useState(0)
const [items, setItems] = useState([])
\`\`\`

**Что здесь происходит:**
- `textInput` - переменная состояния (текущее значение)
- `setTextInput` - функция для обновления этой переменной
- При вызове `setTextInput`, компонент перерендеривается с новым значением

### Array.concat - Добавление элементов

\`\`\`javascript
setItems(items.concat(newItems))
\`\`\`

**Почему concat, а не push?**
- `concat` создает **новый** массив (не мутирует старый)
- `push` изменяет **существующий** массив (мутация)
- В React нужно избегать мутаций состояния!

### Array.filter - Удаление элементов

\`\`\`javascript
const updatedItems = items.filter((item) => item.id !== id)
\`\`\`

**Что здесь происходит:**
- `filter` проходит по каждому элементу
- Оставляет только элементы, где `item.id !== id`
- Возвращает новый массив без удаленного элемента

### Array.map - Создание списка

\`\`\`javascript
{items.map((item) => (
  <ListItem
    key={item.id}
    item={item}
    onDelete={handleDeleteItem}
  />
))}
\`\`\`

**Что здесь происходит:**
- `map` проходит по каждому элементу в массиве
- Для каждого элемента создает компонент `ListItem`
- **key** - уникальный идентификатор для React (важен для оптимизации!)

## 💡 Примеры использования

### Пример 1: Добавление 3 элементов
- Текст: `"Учебный материал"`
- Количество: `3`
- Результат: В списке появятся 3 элемента с текстом "Учебный материал"

### Пример 2: Удаление элемента
- Нажмите кнопку "🗑️ Удалить" на любом элементе
- Элемент сразу исчезнет из списка

## 🎓 Учебные цели

Этот проект помогает научиться:
1. ✅ Создавать функциональные компоненты
2. ✅ Использовать хук useState
3. ✅ Обрабатывать события (onClick, onChange)
4. ✅ Работать с массивами (concat, filter, map)
5. ✅ Передавать props между компонентами
6. ✅ Управлять состоянием приложения
7. ✅ Писать чистый и понятный код

## 📝 Автор

Создано в рамках курса **"Введение в React, первое взаимодействие с backend"**

## 📞 Контакты

Если у вас есть вопросы, обратитесь к преподавателю курса.

---

**Happy Coding! 🚀**
